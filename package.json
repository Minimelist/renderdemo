{
  "name": "task-manager-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build-node": "cd backend-node && npm install",
    "start-node": "cd backend-node && node server.js",
    "build-python": "cd backend-python && pip install -r requirements.txt",
    "start-python": "cd backend-python && gunicorn app:app",
    "build-frontend": "cd frontend && npm install && npm run build"
  }
}